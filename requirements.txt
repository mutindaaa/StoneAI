# Core Dependencies
ultralytics>=8.0.0
supervision>=0.22.0
opencv-python>=4.8.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Deep Learning (CUDA-enabled PyTorch)
# Install with: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=2.0.0
torchvision>=0.15.0

# Roboflow Sports — pitch config, annotators, ViewTransformer
# Also installs: transformers, umap-learn, sentencepiece, protobuf, tqdm
sports @ git+https://github.com/roboflow/sports.git

# SigLIP team classification
transformers>=4.37.0
umap-learn>=0.5.5

# Jersey Number Recognition (Optional but recommended)
easyocr>=1.7.0

# Video Utilities
yt-dlp  # For downloading YouTube videos

# API Server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9  # multipart file uploads for FastAPI

# Dashboard
streamlit>=1.32.0

# Data analytics layer
kloppy>=3.14.0                  # Multi-provider event/tracking data ingestion + normalization
mplsoccer>=1.3.0                # Pitch visualization (shot maps, heatmaps, pass networks, pizza charts)
statsbombpy>=1.0.3              # StatsBomb open data loader
socceraction>=1.3.0             # VAEP + xT player valuation; SPADL event format
soccerdata>=1.8.0               # Scrapes FBref, Understat, WhoScored into Pandas DataFrames
norfair>=2.2.0                  # Lightweight multi-object tracker (complements ByteTrack)
matplotlib>=3.7.0               # Required by mplsoccer
